class Yprops {
    	public  readonly a = "a"
	public  readonly abbr = "abbr"
	public  readonly address = "address"
	public  readonly area = "area"
	public  readonly article = "article"
	public  readonly aside = "aside"
	public  readonly audio = "audio"
	public  readonly b = "b"
	public  readonly base = "base"
	public  readonly bdi = "bdi"
	public  readonly bdo = "bdo"
	public  readonly blockquote = "blockquote"
	public  readonly body = "body"
	public  readonly br = "br"
	public  readonly button = "button"
	public  readonly canvas = "canvas"
	public  readonly caption = "caption"
	public  readonly cite = "cite"
	public  readonly co = "co"
	public  readonly col = "col"
	public  readonly colgroup = "colgroup"
	public  readonly data = "data"
	public  readonly datalist = "datalist"
	public  readonly dd = "dd"
	public  readonly del = "del"
	public  readonly details = "details"
	public  readonly dfn = "dfn"
	public  readonly dialog = "dialog"
	public  readonly div = "div"
	public  readonly dl = "dl"
	public  readonly dt = "dt"
	public  readonly em = "em"
	public  readonly embed = "embed"
	public  readonly fieldset = "fieldset"
	public  readonly figcaption = "figcaption"
	public  readonly figure = "figure"
	public  readonly footer = "footer"
	public  readonly form = "form"
	public  readonly h1 = "h1"
	public  readonly h2 = "h2"
	public  readonly h3 = "h3"
	public  readonly h4 = "h4"
	public  readonly h5 = "h5"
	public  readonly h6 = "h6"
	public  readonly head = "head"
	public  readonly header = "header"
	public  readonly hgroup = "hgroup"
	public  readonly hr = "hr"
	public  readonly html = "html"
	public  readonly i = "i"
	public  readonly iframe = "iframe"
	public  readonly img = "img"
	public  readonly input = "input"
	public  readonly ins = "ins"
	public  readonly kbd = "kbd"
	public  readonly label = "label"
	public  readonly legend = "legend"
	public  readonly li = "li"
	public  readonly link = "link"
	public  readonly main = "main"
	public  readonly map = "map"
	public  readonly mark = "mark"
	public  readonly menu = "menu"
	public  readonly meta = "meta"
	public  readonly meter = "meter"
	public  readonly nav = "nav"
	public  readonly noscript = "noscript"
	public  readonly object = "object"
	public  readonly ol = "ol"
	public  readonly optgroup = "optgroup"
	public  readonly option = "option"
	public  readonly output = "output"
	public  readonly p = "p"
	public  readonly picture = "picture"
	public  readonly pre = "pre"
	public  readonly progress = "progress"
	public  readonly q = "q"
	public  readonly rp = "rp"
	public  readonly rt = "rt"
	public  readonly ruby = "ruby"
	public  readonly s = "s"
	public  readonly samp = "samp"
	public  readonly script = "script"
	public  readonly section = "section"
	public  readonly select = "select"
	public  readonly slot = "slot"
	public  readonly small = "small"
	public  readonly source = "source"
	public  readonly span = "span"
	public  readonly strong = "strong"
	public  readonly style = "style"
	public  readonly sub = "sub"
	public  readonly summary = "summary"
	public  readonly sup = "sup"
	public  readonly table = "table"
	public  readonly tbody = "tbody"
	public  readonly td = "td"
	public  readonly template = "template"
	public  readonly textarea = "textarea"
	public  readonly tfoot = "tfoot"
	public  readonly th = "th"
	public  readonly thead = "thead"
	public  readonly time = "time"
	public  readonly title = "title"
	public  readonly tr = "tr"
	public  readonly track = "track"
	public  readonly u = "u"
	public  readonly ul = "ul"
	public  readonly var = "var"
	public  readonly video = "video"
	public  readonly wbr = "wbr"

}

class Y {

	public static readonly a = "a"
	public static readonly abbr = "abbr"
	public static readonly address = "address"
	public static readonly area = "area"
	public static readonly article = "article"
	public static readonly aside = "aside"
	public static readonly audio = "audio"
	public static readonly b = "b"
	public static readonly base = "base"
	public static readonly bdi = "bdi"
	public static readonly bdo = "bdo"
	public static readonly blockquote = "blockquote"
	public static readonly body = "body"
	public static readonly br = "br"
	public static readonly button = "button"
	public static readonly canvas = "canvas"
	public static readonly caption = "caption"
	public static readonly cite = "cite"
	public static readonly co = "co"
	public static readonly col = "col"
	public static readonly colgroup = "colgroup"
	public static readonly data = "data"
	public static readonly datalist = "datalist"
	public static readonly dd = "dd"
	public static readonly del = "del"
	public static readonly details = "details"
	public static readonly dfn = "dfn"
	public static readonly dialog = "dialog"
	public static readonly div = "div"
	public static readonly dl = "dl"
	public static readonly dt = "dt"
	public static readonly em = "em"
	public static readonly embed = "embed"
	public static readonly fieldset = "fieldset"
	public static readonly figcaption = "figcaption"
	public static readonly figure = "figure"
	public static readonly footer = "footer"
	public static readonly form = "form"
	public static readonly h1 = "h1"
	public static readonly h2 = "h2"
	public static readonly h3 = "h3"
	public static readonly h4 = "h4"
	public static readonly h5 = "h5"
	public static readonly h6 = "h6"
	public static readonly head = "head"
	public static readonly header = "header"
	public static readonly hgroup = "hgroup"
	public static readonly hr = "hr"
	public static readonly html = "html"
	public static readonly i = "i"
	public static readonly iframe = "iframe"
	public static readonly img = "img"
	public static readonly input = "input"
	public static readonly ins = "ins"
	public static readonly kbd = "kbd"
	public static readonly label = "label"
	public static readonly legend = "legend"
	public static readonly li = "li"
	public static readonly link = "link"
	public static readonly main = "main"
	public static readonly map = "map"
	public static readonly mark = "mark"
	public static readonly menu = "menu"
	public static readonly meta = "meta"
	public static readonly meter = "meter"
	public static readonly nav = "nav"
	public static readonly noscript = "noscript"
	public static readonly object = "object"
	public static readonly ol = "ol"
	public static readonly optgroup = "optgroup"
	public static readonly option = "option"
	public static readonly output = "output"
	public static readonly p = "p"
	public static readonly picture = "picture"
	public static readonly pre = "pre"
	public static readonly progress = "progress"
	public static readonly q = "q"
	public static readonly rp = "rp"
	public static readonly rt = "rt"
	public static readonly ruby = "ruby"
	public static readonly s = "s"
	public static readonly samp = "samp"
	public static readonly script = "script"
	public static readonly section = "section"
	public static readonly select = "select"
	public static readonly slot = "slot"
	public static readonly small = "small"
	public static readonly source = "source"
	public static readonly span = "span"
	public static readonly strong = "strong"
	public static readonly style = "style"
	public static readonly sub = "sub"
	public static readonly summary = "summary"
	public static readonly sup = "sup"
	public static readonly table = "table"
	public static readonly tbody = "tbody"
	public static readonly td = "td"
	public static readonly template = "template"
	public static readonly textarea = "textarea"
	public static readonly tfoot = "tfoot"
	public static readonly th = "th"
	public static readonly thead = "thead"
	public static readonly time = "time"
	public static readonly title = "title"
	public static readonly tr = "tr"
	public static readonly track = "track"
	public static readonly u = "u"
	public static readonly ul = "ul"
	public static readonly var = "var"
	public static readonly video = "video"
	public static readonly wbr = "wbr"

}




const _a = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.a,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _abbr = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.abbr,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _address = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.address,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _area = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.area,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _article = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.article,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _aside = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.aside,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _audio = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.audio,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _b = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.b,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _base = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.base,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _bdi = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.bdi,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _bdo = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.bdo,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _blockquote = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.blockquote,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _body = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.body,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _br = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.br,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _button = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.button,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _canvas = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.canvas,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _caption = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.caption,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _cite = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.cite,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _co = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.co,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _col = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.col,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _colgroup = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.colgroup,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _data = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.data,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _datalist = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.datalist,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _dd = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.dd,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _del = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.del,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _details = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.details,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _dfn = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.dfn,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _dialog = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.dialog,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _div = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.div,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _dl = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.dl,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _dt = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.dt,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _em = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.em,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _embed = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.embed,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _fieldset = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.fieldset,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _figcaption = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.figcaption,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _figure = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.figure,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _footer = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.footer,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _form = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.form,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _h1 = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.h1,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _h2 = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.h2,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _h3 = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.h3,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _h4 = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.h4,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _h5 = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.h5,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _h6 = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.h6,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _head = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.head,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _header = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.header,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _hgroup = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.hgroup,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _hr = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.hr,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _html = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.html,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _i = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.i,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _iframe = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.iframe,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _img = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.img,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _input = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.input,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _ins = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.ins,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _kbd = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.kbd,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _label = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.label,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _legend = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.legend,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _li = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.li,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _link = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.link,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _main = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.main,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _map = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.map,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _mark = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.mark,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _menu = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.menu,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _meta = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.meta,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _meter = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.meter,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _nav = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.nav,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _noscript = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.noscript,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _object = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.object,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _ol = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.ol,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _optgroup = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.optgroup,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _option = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.option,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _output = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.output,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _p = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.p,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _picture = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.picture,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _pre = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.pre,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _progress = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.progress,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _q = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.q,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _rp = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.rp,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _rt = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.rt,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _ruby = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.ruby,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _s = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.s,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _samp = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.samp,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _script = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.script,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _section = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.section,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _select = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.select,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _slot = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.slot,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _small = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.small,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _source = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.source,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _span = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.span,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _strong = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.strong,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _style = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.style,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _sub = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.sub,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _summary = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.summary,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _sup = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.sup,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _table = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.table,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _tbody = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.tbody,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _td = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.td,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _template = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.template,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _textarea = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.textarea,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _tfoot = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.tfoot,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _th = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.th,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _thead = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.thead,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _time = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.time,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _title = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.title,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _tr = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.tr,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _track = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.track,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _u = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.u,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _ul = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.ul,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _var = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.var,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _video = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.video,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};




const _wbr = (...args:Array<string | YoFiElement | attrTtype | ((elem:YoFiElement) => void)>) => {
    var attrs
    var cheldren = []
    var init
    var textContent = ""
    for(let arg of args){
        if(typeof arg == "function"){
            init = arg
            continue;
        }
        if(arg instanceof YoFiElement){
            cheldren.push(arg)
            continue;
        }
        if(typeof arg == "string"){
            textContent += arg
            continue;
        }
        if(arg instanceof Object){
            attrs = arg
        }
    }
    return new YoFiElement({
        tag:Y.wbr,
        attrs:attrs,
        cheldren:cheldren ? (cheldren as any).filter((value:any) => typeof value != "string") : undefined,
        init:init,
        textContent:textContent
    })
};
